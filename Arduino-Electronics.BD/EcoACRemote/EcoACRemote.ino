/*uint64_t tRawDataOn[]={0x324000126CB23, 0x3C0000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1600, 550, 1050, 550, 300, &tRawDataOn[0], 112, PROTOCOL_IS_LSB_FIRST, 0,0);
delay(10000);
   uint64_t tRawDataOff[]={0x320000126CB23, 0x380000000000};
   IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1600, 550, 1050, 550, 300, &tRawDataOff[0], 112, PROTOCOL_IS_LSB_FIRST, 0,0);
    //IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1550, 550, 1050, 550, 300, &tRawDataOff[0], 112, PROTOCOL_IS_LSB_FIRST,0,0);
  //31
  uint64_t tRawData[]={0x324000126CB23, 0x3C0000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1550, 550, 1100, 550, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, 0, 0);
  //30
  uint64_t tRawData[]={0x10324000126CB23, 0x3D0000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1550, 550, 1100, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, 0, 0);
    //29
    uint64_t tRawData[]={0x20324000126CB23, 0x3E0000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1650, 500, 1100, 500, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //28
    uint64_t tRawData[]={0x30324000126CB23, 0x3F0000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1650, 550, 1100, 550, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //27
    uint64_t tRawData[]={0x40324000126CB23, 0x400000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1600, 550, 1100, 550, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //26
    uint64_t tRawData[]={0x50324000126CB23, 0x410000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1600, 550, 1100, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //25
    uint64_t tRawData[]={0x60324000126CB23, 0x420000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1600, 550, 1050, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //24
    uint64_t tRawData[]={0x70324000126CB23, 0x430000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1600, 550, 1050, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //23
    uint64_t tRawData[]={0x80324000126CB23, 0x440000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1550, 550, 1050, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //22
    uint64_t tRawData[]={0x90324000126CB23, 0x450000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1600, 550, 1100, 550, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //21
    uint64_t tRawData[]={0xA0324000126CB23, 0x460000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1600, 550, 1050, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //20
    uint64_t tRawData[]={0xB0324000126CB23, 0x470000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1550, 550, 1050, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //19
    uint64_t tRawData[]={0xC0324000126CB23, 0x480000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1600, 550, 1100, 550, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //18
    uint64_t tRawData[]={0xD0324000126CB23, 0x490000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1600, 550, 1100, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //17
    uint64_t tRawData[]={0xE0324000126CB23, 0x4A0000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1550, 550, 1100, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //16
    uint64_t tRawData[]={0xF0324000126CB23, 0x4B0000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1600, 550, 1050, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //sleep act
    uint64_t tRawData[]={0xB0324000126CB23, 0x480000000001};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1600, 550, 1100, 550, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //fan
    uint64_t tRawData[]={0xB0324000126CB23, 0x4C0000000005};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1600, 550, 1100, 550, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //swing
    uint64_t tRawData[]={0xB0324000126CB23, 0x7F0000000038};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1650, 550, 1100, 550, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
*/
/*
 * Pin mapping table for different platforms
 *
 * Platform     IR input    IR output   Tone      Core/Pin schema
 * --------------------------------------------------------------
 * DEFAULT/AVR  2           3           4         Arduino
 * ESP8266      14|D5       12|D6       %
 * ESP32        15          4           27
 */
#include <Arduino.h>

#include "PinDefinitionsAndMore.h"  // Define macros for input and output pin etc.

#define DISABLE_CODE_FOR_RECEIVER // Disables restarting receiver after each send. Saves 450 bytes program memory and 269 bytes RAM if receiving functions are not used.

#include <IRremote.hpp>
#include <PubSubClient.h>
#define BUILTIN_LED 2
#define DELAY_AFTER_SEND 2000
#define DELAY_AFTER_LOOP 5000
const char* ssid = "Sarah";
const char* password = "abcd1234efgh";
const char* mqtt_server = "192.168.0.199";
#if defined(ESP8266)
WiFiClient espClient;
PubSubClient client(espClient);
unsigned long lastMsg = 0;
#define MSG_BUFFER_SIZE	(50)
char msg[MSG_BUFFER_SIZE];
int value = 0;

void setup_wifi() {

  delay(10);
  // We start by connecting to a WiFi network
  Serial.println();
  Serial.print("Connecting to ");
  Serial.println(ssid);

  WiFi.mode(WIFI_STA);
  WiFi.begin(ssid, password);

  while (WiFi.status() != WL_CONNECTED) {
    delay(500);
    Serial.print(".");
  }

  randomSeed(micros());

  Serial.println("");
  Serial.println("WiFi connected");
  Serial.println("IP address: ");
  Serial.println(WiFi.localIP());
}

void callback(char* topic, byte* payload, unsigned int length) {
  Serial.print("Message arrived [");
  Serial.print(topic);
  Serial.print("] ");
  for (int i = 0; i < length; i++) {
    Serial.print((char)payload[i]);
  }
  Serial.println();

  // Switch on the LED if an 1 was received as first character
  if ((char)payload[0] == '8') {
    digitalWrite(BUILTIN_LED, LOW);   // Turn the LED on (Note that LOW is the voltage level
    // but actually the LED is on; this is because
    // it is active low on the ESP-01)
  } else if ((char)payload[0] == '9') {
    digitalWrite(BUILTIN_LED, HIGH);  // Turn the LED off by making the voltage HIGH
  }

}

void reconnect() {
  // Loop until we're reconnected
  while (!client.connected()) {
    Serial.print("Attempting MQTT connection...");
    // Create a random client ID
    String clientId = "ESP8266Client-";
    clientId += String(random(0xffff), HEX);
    // Attempt to connect
    if (client.connect(clientId.c_str())) {
      Serial.println("connected");
      // Once connected, publish an announcement...
      client.publish("ECOAC/Remote", "hello world");
      // ... and resubscribe
      client.subscribe("ECOAC/Remote");
    } else {
      Serial.print("failed, rc=");
      Serial.print(client.state());
      Serial.println(" try again in 5 seconds");
      // Wait 5 seconds before retrying
      delay(5000);
    }
  }
}
#endif
void setup() {
  pinMode(BUILTIN_LED, OUTPUT);     // Initialize the BUILTIN_LED pin as an output
    Serial.begin(115200);
    // Just to know which program is running on my Arduino
    Serial.println(F("START " __FILE__ " from " __DATE__ "\r\nUsing library version " VERSION_IRREMOTE));
  #if defined(ESP8266)
  setup_wifi();
  client.setServer(mqtt_server, 1883);
  client.setCallback(callback);
  #endif
#if defined(IR_SEND_PIN)
    IrSender.begin(); // Start with IR_SEND_PIN as send pin and enable feedback LED at default feedback LED pin
#  if defined(IR_SEND_PIN_STRING)
    Serial.println(F("Send IR signals at pin " IR_SEND_PIN_STRING));
#  else
    Serial.println(F("Send IR signals at pin " STR(IR_SEND_PIN)));
#  endif
#else
    IrSender.begin(3, ENABLE_LED_FEEDBACK, USE_DEFAULT_FEEDBACK_LED_PIN); // Specify send pin and enable feedback LED at default feedback LED pin
    Serial.println(F("Send IR signals at pin 3"));
#endif

#if !defined(SEND_PWM_BY_TIMER)
    /*
     * Print internal software PWM signal generation info
     */
    IrSender.enableIROut(38); // Call it with 38 kHz just to initialize the values printed below
    Serial.print(F("Send signal mark duration is "));
    Serial.print(IrSender.periodOnTimeMicros);
    Serial.print(F(" us, pulse narrowing correction is "));
    Serial.print(IrSender.getPulseCorrectionNanos());
    Serial.print(F(" ns, total period is "));
    Serial.print(IrSender.periodTimeMicros);
    Serial.println(F(" us"));
#endif
}

void loop() {
  #if defined(ESP8266)
  if (!client.connected()) {
    reconnect();
  }
  client.loop();
  #endif
  uint64_t tRawDataOn[]={0x324000126CB23, 0x3C0000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1600, 550, 1050, 550, 300, &tRawDataOn[0], 112, PROTOCOL_IS_LSB_FIRST, 0,0);
delay(10000);
   uint64_t tRawDataOff[]={0x320000126CB23, 0x380000000000};
   IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1600, 550, 1050, 550, 300, &tRawDataOff[0], 112, PROTOCOL_IS_LSB_FIRST, 0,0);
    //IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1550, 550, 1050, 550, 300, &tRawDataOff[0], 112, PROTOCOL_IS_LSB_FIRST,0,0);
  /*//31
  uint64_t tRawData[]={0x324000126CB23, 0x3C0000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1550, 550, 1100, 550, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, 0, 0);
  //30
  uint64_t tRawData[]={0x10324000126CB23, 0x3D0000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1550, 550, 1100, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, 0, 0);
    //29
    uint64_t tRawData[]={0x20324000126CB23, 0x3E0000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1650, 500, 1100, 500, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //28
    uint64_t tRawData[]={0x30324000126CB23, 0x3F0000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1650, 550, 1100, 550, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //27
    uint64_t tRawData[]={0x40324000126CB23, 0x400000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1600, 550, 1100, 550, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //26
    uint64_t tRawData[]={0x50324000126CB23, 0x410000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1600, 550, 1100, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //25
    uint64_t tRawData[]={0x60324000126CB23, 0x420000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1600, 550, 1050, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //24
    uint64_t tRawData[]={0x70324000126CB23, 0x430000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1600, 550, 1050, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //23
    uint64_t tRawData[]={0x80324000126CB23, 0x440000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1550, 550, 1050, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //22
    uint64_t tRawData[]={0x90324000126CB23, 0x450000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1600, 550, 1100, 550, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //21
    uint64_t tRawData[]={0xA0324000126CB23, 0x460000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1600, 550, 1050, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //20
    uint64_t tRawData[]={0xB0324000126CB23, 0x470000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1550, 550, 1050, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //19
    uint64_t tRawData[]={0xC0324000126CB23, 0x480000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1600, 550, 1100, 550, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //18
    uint64_t tRawData[]={0xD0324000126CB23, 0x490000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1600, 550, 1100, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //17
    uint64_t tRawData[]={0xE0324000126CB23, 0x4A0000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1550, 550, 1100, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //16
    uint64_t tRawData[]={0xF0324000126CB23, 0x4B0000000000};
    IrSender.sendPulseDistanceWidthFromArray(38, 3150, 1600, 550, 1050, 550, 250, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //sleep act
    uint64_t tRawData[]={0xB0324000126CB23, 0x480000000001};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1600, 550, 1100, 550, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //fan
    uint64_t tRawData[]={0xB0324000126CB23, 0x4C0000000005};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1600, 550, 1100, 550, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
    //swing
    uint64_t tRawData[]={0xB0324000126CB23, 0x7F0000000038};
    IrSender.sendPulseDistanceWidthFromArray(38, 3100, 1650, 550, 1100, 550, 300, &tRawData[0], 112, PROTOCOL_IS_LSB_FIRST, <RepeatPeriodMillis>, <numberOfRepeats>);
*/
    delay(10000);
}

